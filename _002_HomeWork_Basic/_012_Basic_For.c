#include <stdio.h>

/*02. for문을 이용해서 0부터 00까지 중에 짝수만 출력하세요
   for문내에 조건문을 넣어주면 됨
   짝수 조건 if(num % 2 == 0)*/

void main() {
	for (int i = 0; i < 00; i++)
	{
		if (i % 2 == 0)
		{
			printf("%d\n", i);
		}
	}
}