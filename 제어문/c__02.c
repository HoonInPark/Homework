/*5개의 값을 입력받고
   최대값과 최소값을 추출하세요 */
#include "stdio.h"

void main() {
	float a, b, c, d, e;
	double i;
	printf("5개의 값을 입력하세요.");
	scanf_s("%f %f %f %f %f", &a, &b, &c, &d, &e);
	for (i = 0; i < 5; i++) {

	}
}